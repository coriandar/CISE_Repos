name: CI_action

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14, 16]

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install
        working-directory: worksheet02/vite-frontend

      - name: Run build
        run: npm run build
        working-directory: worksheet02/vite-frontend

      - name: Run tests
        run: npm test
        working-directory: worksheet02/vite-frontend